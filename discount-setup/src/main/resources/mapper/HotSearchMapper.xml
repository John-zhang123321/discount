<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zl.setup.mapper.HotSearchMapper">

    <select id="getByParam" resultType="com.zl.setup.bo.HotSearchBO">
        select
            id,
            count 
        from hot_search
        <where>
        </where>
        order by create_time desc
    </select>
</mapper>